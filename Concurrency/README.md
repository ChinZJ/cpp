# Concurrency

## Version
Mostly running on C++ 20.
To be compiled with gcc

## Build Tools
cmake and ninja
`sudo apt install -y cmake ninja-build ccache`

## Static Analysis and Formatting
`sudo apt install -y clang-tidy cppcheck clang-format`